{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "TicketFlow (Docker)",
            "url": "http://127.0.0.1:8090",
            "webRoot": "${workspaceFolder}/frontend"
        }
    ]
}
